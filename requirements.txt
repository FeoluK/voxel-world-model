# Core (training/eval ran on CUDA, torch 2.x, 4×L40S under torchrun DDP)
torch>=2.1
numpy
# Data/eval utilities
einops
tqdm
# Note: training data, encoded latents, and model checkpoints are NOT in this
# repo (cluster storage). This repo is a code + results checkpoint.
